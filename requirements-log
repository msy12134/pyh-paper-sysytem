现在我的这个test.py脚本可以顺利读取net.csv中的信息，从而构建出120个交换机构成的拓扑。但是这个构建的过程太慢了，我需要先构建一个小一点的拓扑，总共
36颗卫星，四个自治域每个九颗卫星。这个小拓扑暂时称为demo.py

1.初始化的时候，保证每个自治域内的从属卫星是可以与域内的控制卫星通过ipv4互ping
2.当域内卫星相互通信的时候，如果卫星收到的数据包没有路由表可以匹配，就会向控制卫星发送请求，控制卫星收到请求后将相应的路由表信息传送给请求卫星，这样请求卫星
就能知道这个数据包该往哪里传送。

预缓存
某个卫星A快要进入热点区域之前，向自己域内控制器传达一个信号，然后域内控制器也向总控制器传达信号，接着总控制器将路由信息广播给所有控制器（每个自治域内的控制器到
卫星A的路由路径）
之后如果其他域内卫星要与卫星A通信，直接问自己域内的控制器得到路由信息即可
从而实现预缓存基础上的域间通信
